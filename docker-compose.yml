services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: recruitflow
      POSTGRES_PASSWORD: recruitflow
      POSTGRES_DB: recruitflow
    ports:
      - "5432:5432"
    volumes:
      - recruitflow_db:/var/lib/postgresql/data
volumes:
  recruitflow_db:
